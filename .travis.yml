language: node_js
node_js:
  - "iojs"
sudo: required
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y erlang
  - epmd -daemon
script:
  - npm run lint
  - npm run test-coveralls
